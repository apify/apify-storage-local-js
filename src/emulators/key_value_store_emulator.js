const { STORAGE_NAMES, TIMESTAMP_SQL } = require('../consts');

class KeyValueStoreEmulator {
    constructor(storageDir) {
        this.dir = storageDir;
    }

    getStats
}

module.exports = KeyValueStoreEmulator;
