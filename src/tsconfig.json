{
    "extends": "../tsconfig.base.json",
    "compilerOptions": {
        "rootDir": "./",
        "outDir": "../dist",
        "composite": true,
        "preserveConstEnums": true,

        // Remove this once we finish migration from JS
        "allowJs": true
    },
    "include": ["."]
}
